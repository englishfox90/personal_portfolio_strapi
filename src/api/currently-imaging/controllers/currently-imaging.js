'use strict';

/**
 * currently-imaging controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::currently-imaging.currently-imaging');
